apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata
spec:
  replicas: 1
  selector:
    matchLabels:
      app: metadata
  template:
    metadata:
      labels:
        app: metadata
    spec:
      containers:
      - name: metadata
        image: jovial5/cinephile-metadata:1.0.0
        imagePullPolicy: IfNotPresent
        ports:
          - containerPort: 8081
